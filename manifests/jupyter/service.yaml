apiVersion: v1
kind: Service
metadata:
  name: jupyter
  namespace: jupyter
  annotations:
    # Service-mode tailnet exposure -> jupyter.taildd208.ts.net, same
    # proven pattern as manifests/gruesome and manifests/open-webui.
    tailscale.com/expose: "true"
    tailscale.com/hostname: "jupyter"
spec:
  selector:
    app: jupyter
  ports:
    - port: 80
      targetPort: 8888
