apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-config
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./manifests/metallb-config
  prune: true
  dependsOn:
    - name: metallb
  sourceRef:
    kind: GitRepository
    name: flux-system
