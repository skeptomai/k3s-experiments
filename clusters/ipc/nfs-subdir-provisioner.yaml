apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nfs-subdir-provisioner
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./manifests/nfs-subdir-provisioner
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
