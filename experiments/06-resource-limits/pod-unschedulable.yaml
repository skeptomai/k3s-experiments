apiVersion: v1
kind: Pod
metadata:
  name: unschedulable-demo
  namespace: limits-demo
spec:
  containers:
    - name: nginx
      image: nginx:stable
      resources:
        requests:
          cpu: "100"
          memory: "500Gi"
  restartPolicy: Never
