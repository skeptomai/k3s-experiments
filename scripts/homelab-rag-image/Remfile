FROM python:3.14-slim

RUN apt-get update -qq && apt-get install -y -qq --no-install-recommends openssh-client \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir "pydantic-ai>=2.43.0" asyncpg

WORKDIR /app
COPY app.py .

EXPOSE 8080
CMD ["python3", "app.py"]
